public enum AttackType {
    Basic,          // Standard attack, unit attack + attack value vs target defense values
    IgnoreDefense,  // Attack that ignores the target's defense
    HealAlly,     // Heal target ally by attack value
    //...
}
